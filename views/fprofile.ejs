<!doctype html>
<html>

<head>
    <title>Facebook Profile</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" crossorigin="anonymous">
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css">
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"
        integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p"
        crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.min.js"
        integrity="sha384-Atwg2Pkwv9vp0ygtn1JAojH0nYbwNJLPhwyoVbhoPwBhjQPR5VtM2+xf0Uwh9KtT"
        crossorigin="anonymous"></script>
    <style>
        body {
            padding-top: 80px;
            background: url('background.jpg') no-repeat center center fixed;
            -webkit-background-size: cover;
            -moz-background-size: cover;
            -o-background-size: cover;
            background-size: cover;
        }

        .card {
            margin: 0 auto;
            float: none;
            background: transparent;
            width: 27rem;
            height: 22rem;
            opacity: 0.8;
        }

        .dropdown-image {
            width: 50px;
            height: 50px;
            border-radius: 100%;
            -webkit-animation: spin 2s normal infinite;
            -moz-animation: spin 2s normal infinite;
            animation: spin 2s normal infinite;
        }

        @-webkit-keyframes spin {
            100% {
                -webkit-transform: rotate(360deg);
            }
        }

        @-moz-keyframes spin {
            100% {
                -webkit-transform: rotate(360deg);
            }
        }

        @keyframes spin {
            100% {
                -webkit-transform: rotate(360deg);
            }
        }

        .display-image {
            border-radius: 100%;
            width: 100px;
            height: 100px;
        }

        .dropdown {
            margin-bottom: 40px;
        }

        h5 {
            color: darkblue;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="dropdown">
            <button class="btn btn-light dropup" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown"
                aria-expanded="false">
                <img src="<%= user.pic %> " class="dropdown-image"><span style="color: darkblue;"><strong> Hi <%=
                            user.fname %></strong></span>
            </button>
            <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
                <li><a href="/logout" class="dropdown-item">Logout</a></li>
            </ul>
        </div>
        <div class="text-center">
            <div class="card shadow-lg p-3 mb-5 bg-body rounded">
                <img src="<%= user.pic %>" class="display-image mx-auto d-block card-img-top"><br>
                <div class="card-header">
                    <h5><strong>User-Id: <%= user.uid %></strong>
                    </h5><br>
                    <h5><strong>Name: <%= user.name %></strong>
                    </h5><br>
                    <h5><strong>Email-Id: <%= user.email %></strong>
                    </h5><br>
                </div>
            </div>
        </div>
    </div>
</body>

</html>